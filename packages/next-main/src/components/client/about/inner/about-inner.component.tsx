'use client';

import React, { FC, memo } from 'react';

type AboutInnerClientProps = {};

const AboutInnerClient: FC<AboutInnerClientProps> = () => {
    return <div>AboutInnerClient</div>;
};

export default memo(AboutInnerClient);
