'use client';

import React, { FC, memo } from 'react';

type MyAboutClientProps = {};

const MyAboutClient: FC<MyAboutClientProps> = () => {
    return <div>MyAboutClient</div>;
};

export default memo(MyAboutClient);
