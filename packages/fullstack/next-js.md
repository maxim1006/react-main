### Routing
По названию файла в pages автоматом создается страничка

### Настраиваем urql для работы с ssr
делаю по докам + надо использовать 
const [{ data, fetching }] = useMeQuery({
        pause: isServer(),
    });
    
чтобы запрос не рендерился на сервере а проходил на клиенте
