import React from 'react';
import ContextButton from './ContextButton';
import ContextField from './ContextField';

export default () => (
    <div>
        <ContextField />
        <ContextButton />
    </div>
);
