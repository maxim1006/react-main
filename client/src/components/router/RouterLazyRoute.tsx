import React, { FC, memo } from 'react';

type RouterLazyRouteProps = {};

const RouterLazyRoute: FC<RouterLazyRouteProps> = () => {
    return <h4>RouterLazyRoute</h4>;
};

export default memo(RouterLazyRoute);
