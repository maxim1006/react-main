import React, { FC, memo } from 'react';

type RouterExactRouteProps = {};

const RouterExactRoute: FC<RouterExactRouteProps> = () => {
    return <h4>RouterExactRoute</h4>;
};

export default memo(RouterExactRoute);
