import styled from 'styled-components';
import { StyledMainColor } from '../../styled-component/StylesColors';

export const StyledFirestoreFamilyList = styled.ul`
    list-style-type: none;
    color: ${StyledMainColor};
`;
